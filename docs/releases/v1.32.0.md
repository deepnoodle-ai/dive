## Highlights

Search now gives agents clearer next steps: `Glob` returns the newest matches,
and `Grep` supports paging, context, and multiline search across backends.
Limits and skipped paths are visible in tool output.

Stopping a run now ends file walks and tool batches promptly. Built-in file
tools report invalid inputs clearly; custom tools must honor their context.

## Pull requests

- Make tool search results and cancellation reliable (#302)
- Stop tool calls when an agent run is cancelled (#301)

## Changelog

### Changed

- **File search now gives clearer, bounded results.** `Glob` selects the newest
  matches; `Grep` supports paging, context, and multiline search. Both flag
  incomplete searches.

### Fixed

- **Cancelling a run now stops tool batches promptly.** File walks stop,
  sequential batches stop dispatching, and parallel batches stop waiting.
- **Tool errors are explicit.** Null inputs and special-file targets fail
  clearly; built-in tools check cancellation and ended batches suppress late events.
